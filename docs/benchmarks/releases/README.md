# Release Benchmarks

This directory stores official benchmark snapshots for released MUG versions.

Unlike temporary benchmark output under `docs/benchmarks/runs/`, `latest.*`,
`logs/`, and `artifacts/`, files in this directory are intended to be versioned
as historical release references.
