# Release Notes

This directory is the standard location for MUG release notes.

Each release should have a dedicated Markdown file named with the release tag,
for example `v1.3.1.md`.

Release notes should summarize user-visible changes, technical changes,
compatibility guarantees, validation performed, and build artifacts.
